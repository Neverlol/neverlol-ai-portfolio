# 上线前优先级规划

> 日期：2026-04-09
> 适用范围：`my-ai-portfolio` 在沈阳面谈前的所有优化、验收与上线决策
> 用途：后续上下文被压缩时，作为统一执行基线，避免优化方向重新发散

---

## 1. 这份文档解决什么问题

这不是一份泛泛的“网站优化清单”。

这份文档只回答一个问题：

**为了支撑 4 月 15 日前的沈阳面谈，以及后续真正上线，这个网站应该先改什么，后改什么，什么现在绝对不能碰。**

后续所有执行动作，都要先对照这份文档归类到 `P0 / P1 / P2`，再决定是否开始动手。

---

## 2. 当前统一判断

### 网站的真实职责

- 这不是简历站，也不是单纯作品集。
- 这是沈阳线下 PMF 验证的销售武器。
- 页面必须在 3 分钟内让前领导和潜在客户理解：
  - 你能帮他接走什么高频业务动作
  - 为什么不是又一个 CRM 壳子
  - 为什么这件事值得先试点

### 当前优化的唯一主线

所有优化都必须同时服务这 3 个目标：

1. **更稳**：能构建、能运行、能验收、能复现。
2. **更快懂**：首屏 5 秒内讲清“这东西帮我多赚钱、少漏单、少操心”。
3. **更好收口**：访客愿意留下线索，且失败时不会直接丢单。

### 当前不能犯的错

- 不能继续把首页写得像“技术百科”。
- 不能把蓝图讲成完整成品。
- 不能在 `build / lint / typecheck / QA` 还没打通时，先去做审美细修。
- 不能让 CTA 失败后没有人工兜底。

---

## 3. 优先级总表

## P0: 上线硬阻塞，必须先解决

这些问题不解决，不进入“真正上线”阶段。

### P0-1. 修通生产构建链路

**现状**
- `npm run build` 在 240 秒窗口内未完成。

**为什么是 P0**
- 不能稳定产出生产构建，就没有资格谈正式上线。

**完成标准**
- `npm run build` 可在可接受时间内稳定完成。
- 构建结果可用于本地生产预览或部署。
- 不再依赖“dev 环境能打开页面”来代替上线验证。

### P0-2. 修通最小质量检查链路

**现状**
- `tsc --noEmit` 在 45 秒窗口内无结果。
- ESLint 在 25 秒窗口内无结果。
- `npm run lint` 目前只有 `eslint`，可信度不足。

**为什么是 P0**
- 没有可重复运行的检查链路，后面每次改动都可能把站点搞坏而不自知。

**完成标准**
- 至少有 1 条稳定的类型检查命令。
- 至少有 1 条稳定的 lint 命令。
- 检查命令能在合理时间内返回结果，而不是卡死。
- 把这些命令写进项目文档，作为后续统一门槛。

### P0-3. 标准化本地运行与验收方式

**现状**
- 本地存在历史残留 `next dev` 进程。
- 同一路径下出现多个 dev 进程并行，占用同一端口。
- 代理设置会干扰本地 HTTP 检测。

**为什么是 P0**
- 运行环境混乱会污染所有后续判断。

**完成标准**
- 形成唯一推荐的本地启动方式。
- 明确端口、代理绕过方式、进程清理方式。
- 后续任何验收都基于同一套启动标准。

### P0-4. 明确 GStack 的可用边界

**现状**
- 本轮尝试中，`browse` 相关链路不可用。

**为什么是 P0**
- 如果工具本身不可用，却继续默认“可以靠 GStack 验收”，后面会反复撞墙。

**完成标准**
- 明确哪些 GStack 能力当前可用，哪些不可用。
- 在浏览器链路修复前，不再把 `browse` 相关技能当成默认验收方案。

---

## P1: 沈阳面谈前必须优化

这些问题不一定阻止上线，但会直接影响沈阳面谈的第一印象和成交效率。

### P1-1. 压首页首屏的信息密度

**现状**
- 首页 Hero 方向是对的，但概念仍然偏多。
- 动态轨道、OpenClaw、交付结构、Demo 说明在前半屏同时抢注意力。

**目标**
- 让第一次进入页面的人，先看懂价值，再愿意往下看。

**完成标准**
- 首屏只聚焦一个主结果句。
- 首屏按钮和行动路径明显。
- 技术锚点保留，但解释内容下沉，不在第一屏堆满。

### P1-2. 优先优化首页入口体验

**现状**
- 实测 `/` 的 `time_starttransfer` 约 `10.25s`。
- `/solutions` 约 `3.31s`，`/campaigns` 约 `1.93s`，说明首页是当前最慢入口页。
- 首页 HTML 约 `159KB`，包含约 `24` 个脚本标签。

**目标**
- 首页必须成为最快被理解、最快可展示的入口，而不是最重的页面。

**完成标准**
- 先查清首页慢在什么位置：数据、客户端组件、脚本量、图片、动画、环境。
- 优先减少首页首屏无关负担。
- 沈阳现场演示时，首页打开不能拖泥带水。

### P1-3. 强化 CTA 的失败兜底

**现状**
- CTA 真实依赖 Firebase `consulting_leads` 写入。
- 失败时只有通用 `alert`，没有明确的人工兜底通道。

**目标**
- 即便 Firebase 出问题，也不能把高意向线索直接丢掉。

**完成标准**
- 提交失败时有明确的人工备用联系方式。
- 最好有微信 / 手机 / 邮件中的至少一种硬兜底。
- 用户知道“下一步该怎么联系你”，而不是只看到失败提示。

### P1-4. 继续压文案的“老板可理解度”

**现状**
- `Portfolio`、`CooperationProcess` 基本已经对齐主线。
- 但首屏和 Demo 区域仍有概念密度偏高的问题。

**目标**
- 任何老板第一眼先看到业务命脉，不先被系统名词淹没。

**完成标准**
- 首页先讲收益，再讲系统，再讲技术承接。
- 继续避免“判断层 / 管理层 / 节点 / 路由”这类内部工作词直接对外输出。

---

## P2: 上线后或第二阶段增强

这些项重要，但不应插队到 P0/P1 前面。

### P2-1. 恢复 GStack 浏览器验收能力

- 修复 `browse` 启动链路。
- 修复 `playwright -> chromium.launch()` 超时问题。
- 让 `/qa-only`、`/benchmark`、`/canary` 真正可跑。

### P2-2. 建立持续性能基线

- 不只是测一次首页。
- 需要把 `/`、`/solutions`、`/campaigns`、`/cases` 纳入持续基线。

### P2-3. 完善线上监控与回归流程

- 构建通过后，再补完整的上线前 smoke check。
- 部署后再补 canary 和趋势记录。

---

## 4. 推荐执行顺序

后续执行必须按这个顺序推进，除非有新的硬证据证明顺序需要调整。

1. **先修 P0-1 / P0-2 / P0-3**
   先把 `build / typecheck / lint / 本地运行` 修通。

2. **再决定 GStack 浏览器链路怎么处理**
   如果 `browse` 继续不可用，就先明确替代验收方案，不要卡死在工具上。

3. **然后做首页减重与首屏压缩**
   先优化最影响沈阳面谈的入口页。

4. **再补 CTA 兜底**
   确保收口入口不会因为后端波动直接丢线索。

5. **最后做一轮统一验收**
   只有在“能构建、能跑、能提交、能讲清”全部满足后，才进入正式上线判断。

---

## 5. 后续执行红线

### 红线 1

**任何新需求，如果不属于 P0/P1，就不能插队。**

### 红线 2

**任何视觉微调，如果不能同时提升“更快懂”或“更好收口”，就先不做。**

### 红线 3

**任何文案改动，都不能把网站重新写回“技术展示页”。**

### 红线 4

**任何页面都不能暗示“整套系统已经完整交付”。**

### 红线 5

**在 GStack 浏览器链路修复前，不把它当默认验收能力。**

---

## 6. GStack 当前部署状态判断

## 已确认不是“空壳”的证据

以下能力已经明确存在，不是只有一个假安装：

- GStack 根目录、技能文档、`browse/dist/browse` 二进制都存在。
- `browse` 能实际尝试启动 bun server，不是纯假命令。
- `playwright` 包可以被解析。
- Chromium 可执行文件存在：
  - `/Users/neverlol/Library/Caches/ms-playwright/chromium-1208/...`
- `bun` 实际已安装在：
  - `~/.bun/bin/bun`

结论：**GStack 不是只有壳，安装主体是真实存在的。**

## 当前没有打通的部分

但这不等于“能力已可用”。

当前至少有这几条链路没打通：

- `bun` 没进默认 `PATH`，导致 `browse` 首次直接报缺失。
- `browse` 启动后会留下 `.gstack/browse.json.lock`。
- 但不会生成 `browse.json` 或 `browse-server.log`。
- 手动起 `bun run src/server.ts` 也会卡住。
- 进一步验证发现，最小化的 `playwright -> chromium.launch()` 在 25 秒窗口内也会超时。

## 结论

**如果你问的是“GStack 有没有真装上”**

答案是：**装上了，不是空壳。**

**如果你问的是“GStack 的 browse / qa-only / benchmark 这类浏览器能力现在能不能稳定使用”**

答案是：**还不能算部署成功。**

更准确的判断是：

> **GStack 主体已安装，但浏览器自动化子能力当前处于“半部署、不可用”状态。**

也就是说：

- 文档、配置、二进制、依赖主体在。
- 但最关键的浏览器运行时没有打通。
- 所以不能把它视为一套“已完成验收的可用 QA 工具链”。

---

## 7. 后续会话使用协议

任何后续会话开始时，先做两件事：

1. 先读 `CONTEXT.md`
2. 再读这份 `PRELAUNCH_PRIORITY_PLAN.md`

然后只回答两个问题：

- 当前任务属于 `P0 / P1 / P2` 哪一类？
- 这个动作是否会帮助“更稳 / 更快懂 / 更好收口”中的至少一个目标？

如果两个问题都答不清，就先不要开工。

---

## 8. 当前建议的下一步

### 首选

先专注把 `P0` 修通：

- `build`
- `typecheck`
- `lint`
- 本地运行标准化

### 第二步

再做首页首屏减重和 CTA 兜底。

### 第三步

浏览器自动化链路单独作为一条技术债修复，不和首页转化优化混在一起。

这样后续推进才不会一边修网站，一边反复被工具环境拖住。
