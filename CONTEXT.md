# 项目上下文 (供 AI Agent 快速对齐)

> **用途**：CodeX / Claude Code / Antigravity 等 AI 工具在新会话开始时读取此文件，30 秒恢复 80% 上下文。
> **更新频率**：每天工作结束后更新"最近关键决策"部分。

---

## 1. 我是谁

Neverlol，37 岁，前 58 同城东北/华东大区运营负责人（数据化管理 + 运营策略输出）。
2026 年 3 月以 **OPC（一人公司）商业系统架构师** 身份回归市场。
核心定位：**将大厂业务经验 Skill 化封装 + 搭载 OpenClaw 自动化引擎**，当前以重销售的中小团队为切入客群，提供 AI 落地微咨询服务。

## 2. 这个项目在做什么

**项目名**：个人门户网站（my-ai-portfolio）
**技术栈**：Next.js + TypeScript + Tailwind CSS + Firebase（交互层）+ Obsidian（内容 SSOT）
**目标**：这不是简历网站，这是 **OPC 微咨询业务的武器展示库 + 获客入口**。

### 当前交付原则（请始终保持）

- **不是卖通用成熟工作流**：网站展示的是方法论、蓝图和第一批可落地模块，真实交付必须基于客户自己的业务流程做定制封装。
- **不是把我的经验生硬套给客户**：要先梳理客户现有工作流，再结合其案例数据、团队经验和当地变量，把这些内容抽象成客户自己的 Skill 知识库。
- **OpenClaw 是承接层，不是卖点本身**：Skill 封装完成后，再由 OpenClaw 承接自动化流程、人机审批和后台编排。
- **首页 Demo 是蓝图层**：用于展示目标人机协同工作流，不代表所有节点都已完成封装。
- **当前优先落地的是 4 个核心模块**：`crm-auto-fill`、`funnel-doctor`、`customer-profiler`、`renewal-watch` 是当前先切入的 4 个高价值节点。对外避免使用“第一轮封装 / 测试阶段”这类会削弱信任的说法。
- **这套系统不是“中小团队专用”**：它本质上是 AI 时代的人机协同工作系统。大团队也适用，只是大团队通常有自己的技术和流程团队。当前选择重销售的中小团队作为目标客群，是因为他们最缺“把经验抽象成系统”的能力。
- **这套系统不是替换拨号系统 / CRM / 飞书**：对外一律要讲“前台继续用熟悉的微信、电话、拨号系统和 CRM，系统在后台帮你盯线索、看卡点、抓风险、推下一步动作”，不要把网站写成“让客户推翻重来换一套新系统”。
- **落地原则必须低阻力**：前线继续用熟悉的微信、电话、拨号系统和 CRM，系统优先接管后台的记录整理、卡点诊断、客户分层、续费预警，而不是要求团队先推翻重来。
- **老板买单的底层原因必须围绕业务命脉表达**：少浪费线索、少漏掉成交机会、少靠主管人肉追进度、少依赖老员工经验、提前发现流失风险，这些比“Skill / OpenClaw / 自动化”更优先。
- **页面叙事顺序红线**：先讲老板最痛的问题，再讲系统具体帮他接走哪些事，最后再讲 Skill 和 OpenClaw 是怎么落地的。
- **整站页面职责已经锁定**：首页负责转化，先打收入、利润、管理效率这些命脉；`/solutions` 负责讲清人和 AI 在全链路里怎么配合；`/campaigns` 负责证明方法论来源；`/capability` 负责证明我有持续优化这套系统的能力；`/category/[id]` 负责让客户看到“先从一个痛点切进去”会长什么样。
- **文案红线必须执行**：对外不要使用“判断层”“管理层”“路由”“节点”“承接层”等专业术语和互联网黑话。这些可以作为内部工作词，但不能直接写进销售文案。
- **表达方式必须足够接地气**：面向传统 B 端老板写文案时，要用最简单的话、最直接的痛点、最具体的结果；一句话只说一件事，让人第一眼就明白“这东西帮我多赚钱、少花钱、少操心”。
- **文案不能被写成“纯大白话”**：对外必须保留 `AI`、`Skill（AI 技能包）`、`OpenClaw（小龙虾）` 这些技术锚点。目标不是去掉技术感，而是做到“老板能听懂的话 + 足够明显的 AI 技术含量”。
- **OpenClaw 必须同时承担两层角色**：一层是实际部署载体，另一层是市场教育后的认知锚点。网站中不能把 OpenClaw 隐掉，只能把它讲得更容易理解。
- **个人背书不能被稀释**：`前 58 同城大区运营负责人`、`高级数据分析师`、`北外管理科学硕士`、`AI 技术落地实践者` 这些标签必须保留，它们是“业务履历 + 数据运营 + 技术能力”这组稀缺组合的核心证明。
- **标题排版规则必须手工设计**：Hero 和大区块标题不能依赖浏览器自然换行，必须主动做断句和分行。否则会显得像“把长句硬塞进大字号里”。
- **括号解释尽量不要塞进标题**：`Skill（AI 技能包）`、`OpenClaw（小龙虾）` 这类解释优先放在副标题、说明文案和卡片正文里。标题只保留最核心的主语，避免臃肿和排版混乱。
- **Hero 只做钩子，不做百科解释**：首页第一屏只负责抓住客户，让他愿意继续往下看。不要在 Hero 同时解释 `Skill 是什么`、`OpenClaw 是什么`、`AI 和人分别负责什么`，这些内容应当分散到下一屏、Demo 和模块区。
- **Skill 的更准确定义**：对外如果需要解释，优先使用“`Skill` 是给 AI 用的技能包，里面封装的是业务经验、规则和动作”这一类表述。不要写成又像讲业务、又像讲技术、但两边都没讲透的话。
- **Hero 标题要优先说结果，不要说太满**：像“`用 OpenClaw 让 AI 真正开始干活`”这种结果表达，比“进入业务协作”更短、更狠、更容易让老板一眼明白。标题宁可少说，也不要一句里同时塞进过多概念。
- **Hero 主标题控制在 2 行内**：第一行讲“封成 Skill”，第二行讲“接进 OpenClaw 跑起来”或“让 AI 开始干活”。不要再把 `Skill / OpenClaw / AI 干活 / 业务协作` 全塞进同一行或同一句里。
- **Hero 结构要分区，不要互相遮挡**：标题区、按钮区、动态曲线区、底部卡片区必须是 4 个清晰层次。曲线要有单独展示区域，不再和底部 3 张卡片抢同一块垂直空间。
- **标题内 Logo 要跟随文字比例**：`OpenClaw` 前的小龙虾 Logo 不能用固定像素大小硬塞，优先按当前标题字体的相对尺寸匹配，避免忽大忽小。

### 网站核心页面

| 路由 | 功能 | 当前状态 |
|:--|:--|:--|
| `/` | 首页：Hero + CooperationProcess（三段式交付）+ PipelineTeaser + Skill 案例 + ConsultingCTA（审计入口） | 3/26 重构完成 |
| `/solutions` | 解决方案页：8 个业务阶段的 AI/人/主管责任分工 | 3/26 新增 |
| `/campaigns` | 58 真实业务沙盘入口 | 已有 |
| `/case/[id]` | 项目详情页 | 已有 |
| `/log/[slug]` | 日志详情页 | 已有 |

### 关键文件

| 文件/目录 | 说明 |
|:--|:--|
| `src/constants/index.ts` | 所有静态案例数据、Bento Box 配置 |
| `src/app/page.tsx` | 首页主组件 |
| `src/app/solutions/` | 解决方案页 |
| `src/components/` | 所有 UI 组件 |
| `scripts/publish-nl.mjs` | Obsidian → Firebase 一键发布脚本 |
| `firebase.json` | Firebase 配置 |
| `CLAUDE.md` | Claude Code 项目规则 |

### 数据架构
- **内容层 (SSOT)**：Obsidian Markdown 文件（位于外脑 `0.Obsidian_Brain/`）
- **交互层**：Firebase Firestore（评论、联系表单）
- **部署**：Vercel

## 3. 最近 3 天的关键决策（请保持更新）

### 2026-03-27
- **新增执行锚点文档**：后续所有“沈阳面谈前 / 真正上线前”的优化动作，统一以 `PRELAUNCH_PRIORITY_PLAN.md` 为优先级基线；任何会话恢复时，除 `CONTEXT.md` 外必须同时读取该文档，避免执行顺序重新发散。
- **今日核心任务**：将 `19-skill开发` 项目中已封装的 4 个 Skill（renewal-watch / funnel-doctor / customer-profiler / crm-auto-fill）替换进网站的 Skill 案例库。同时将首页自动化 Demo 与这 4 个 Skill 对齐。
- **设计分工**：CodeX 负责逻辑和数据对接，UI 打磨和文案由 Antigravity 负责。
- **Demo 定位锁定**：首页 `销售全链路人机协作 Demo` 保留为目标工作流蓝图；不能被包装成“全节点都已成品化”。后续 UI 需显式区分 `人工节点` / `已封装 Skill` / `可定制扩展节点`。
- **4 个 Skill 的站内角色锁定**：这 4 个 Skill 是蓝图中的首批已封装关键节点，而不是完整工作流本身。
- **面向沈阳种子用户的真实交付模型锁定**：梳理其现有工作流 → 提炼团队经验和案例数据 → 结合大厂方法论抽象成 Skill 知识库 → 再由 OpenClaw 承接为自动化方案。
- **网站叙事红线**：卖的是“定制化业务 Skill 封装能力”，不是“直接卖一套现成模板”。
- **第一性原理纠偏锁定**：
  - 不再用“已完成第一轮封装的几个 Skill”这类容易让客户感觉还在测试的话术
  - 不把系统写成“只适用于中小团队”，而是写成 AI 时代的人机协同工作系统
  - 必须强调它与拨号系统 / CRM / 飞书的区别：那些负责记录、触达、协同，这套系统负责在后台帮老板盯线索、看卡点、抓风险、推下一步动作
  - 必须先讲老板最关心的命脉：线索浪费、成交卡点、流失预警、经验依赖、主管追进度成本
  - 必须强调低阻力落地：系统适应团队，而不是团队先去适应系统
- **页面职责图锁定**：
  - 首页先转化，不先炫技术，也不先讲方法论来源
  - `PipelineTeaser / campaigns` 主要承担“证明我为什么有资格做这件事”，不是首页最前排的主销售动作
  - `solutions` 要先讲“这东西到底帮团队接走了什么事”，再讲人机分工
  - `About / capability` 要证明的不只是背景，而是“我能持续优化，不是只做一次原型”
- **文案方式锁定**：
  - 对外不要说“判断层 / 管理层 / 路由 / 节点 / 承接层”这类术语
  - 不说互联网黑话，不说抽象大词，不用客户需要翻译才能懂的表达
  - 统一改用传统老板能直接听懂的话：少漏单、少卡单、少靠主管盯、少浪费线索、让老员工经验能复制
- **技术感表达锁定**：
  - 首页和关键区块必须显式出现 `AI`、`Skill（AI 技能包）`、`OpenClaw（小龙虾）`
  - `OpenClaw` 首次出现时要带小龙虾称呼，必要时配 Logo 一起出现
  - 不把网站写成“没技术含量的咨询介绍页”，而是写成“能真的把 AI 接进业务流程的人机协作系统”
- **排版纪律锁定**：
  - 主区块的主标题和副标题不能继续依赖浏览器随机换行
  - 首页与主子页的副标题统一采用“移动端自然排、桌面端手工断一刀”的方式收口
  - 标题区负责抓人，解释性内容下沉到副标题或正文，不在同一屏里堆满定义和说明
- **详情页语气锁定**：
  - Skill 详情页不能写得像“专门为某一家潜在客户量身定制”的提案页
  - 避免直接点名财税、代账、企服或当前正在接触的目标客户语境，除非只是轻量举例
  - 页面要保持“成熟项目展示页”的距离感，让用户感觉这是现成能力展示，而不是提前给他挖好的坑
  - 详情页标题优先凸显“这个 Skill 解决什么问题”，不优先强化 Skill 的英文名
  - Skill 英文名可以保留在 Demo 或技术说明区，不占标题主视觉
- **Firestore 运行时策略锁定**：
  - 内容页和展示页不应因为 Firestore 短时连不上就把 SDK 告警直接冒成页面错误
  - Firestore 初始化应保持懒加载，只在真正需要评论、线索提交、沙盘配置时再连接
  - 对可降级失败的 Firestore 请求，前端应回退到本地默认数据或空态，并用 `warn` 级别记录，而不是 `console.error`
- **第一轮页面改造已落地**：
  - `HomepageSkillDemo` 已切成 `已封装 Skill / 人工判断 / 可定制扩展` 三层结构
  - `Portfolio` 已替换为 4 个真实 Skill 模块
  - `CooperationProcess` 已改为“工作流梳理 → 规则抽取 → OpenClaw 部署与周期优化”
  - `category/[id]` 已完成第一轮 Skill 详情页重映射，并兼容旧路由别名
- **第二轮收口已落地**：
  - 首页 Demo 已补上图例和节点数量统计，`已封装 / 人工 / 可定制` 的层级更一眼可读
  - 4 个 Skill 详情页已补上 `首批已封装节点` 标签
  - 4 个 Skill 的痛点和说明已往代账 / 报税 / 注销 / 工商变更 / 续费账期等财税语境压了一轮
- **当前剩余工作**：后续需要把“财税代理团队”语境重新拉回更泛化的重销售团队表达，并按“老板最痛的问题 → 系统帮他接走哪些事 → 技术如何落地”的顺序重写首页关键文案。同时维持 `AI / Skill / OpenClaw` 的技术锚点，不要再被写成过于口语化、缺少技术感的版本。

### 2026-03-26
- **首页叙事重构**：Hero 文案从抽象方法论切换为"定制 Skill + OpenClaw 部署"
- **新增 CooperationProcess**：三段式交付流程（审计 → 部署 → 周期优化）
- **新增 /solutions 页面**：拆清 8 个业务阶段里 AI、人、主管各自负责什么
- **PipelineTeaser** 改成"58 真实业务沙盘入口"
- **Portfolio** 重命名为首批业务 Skill 案例
- **ConsultingCTA** 重构为 Skill 审计申请入口
- 提交：`bbd4276`

### 2026-03-25
- **架构迁移完成**：Supabase → Obsidian (SSOT) + Firebase (交互层)
- **portfolio-sync Skill**：封装了"Obsidian → 本地预览 → Firebase 发布"的闭环
- **UI 极简化**：移除冗余 H1、空指标框，统一日期格式

### 战略背景（长期）
- **种子用户**：沈阳前领导的代账创业团队。**Deadline：4/15 前带着 Demo 去沈阳面谈。**
- **网站的终极目的**：让前领导和任何潜在客户在 3 分钟内理解"你能帮我做什么"。
- **真正的护城河**：不是标准化 SaaS，也不是单点 Prompt，而是“业务抽象能力 + 定制化 Skill 封装能力”。
- **对外表达原则**：先讲业务命脉，再讲系统能帮老板具体接走哪些事，再讲当前优先落地的模块，最后讲如何把客户自己的成熟经验封装进系统。对外不用术语，不用黑话，要让传统 B 端老板直接听懂。

## 4. CodeX 使用注意事项

- **你的长项**：代码逻辑、组件对接、数据结构。请集中火力在数据替换和路由对齐。
- **你的短板**：UI 设计审美和营销文案。涉及视觉微调和文案措辞时，标注 `// TODO: UI/文案交由 Antigravity 处理` 即可。
- **双写纪律**：每次有进展必须同步写入 `DEV_JOURNAL.md` 和 `Global_DEV_JOURNAL.md`。
- **部署命令**：`npm run dev`（本地预览）、`firebase deploy`（正式发布——需用户手动确认）。
